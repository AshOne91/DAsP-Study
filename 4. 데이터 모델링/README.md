# 1. 데이터 모델링 이해
## 1. 데이터 모델링 개요
### 1. 데이터 모델링 탄생 배경
시스템 설계의 전 관정을 지원하는 도구
### 2. 모델 정의
대상을 데이터베이스화하기 위한 개념적 도구
### 3. 모델링이 필요한 이유
프로세스 모델링과 데이터 모델링
- 파급효과
- 정보 시스템의 간결한 표현
- 데이터 품질
### 4. 데이터 모델링의 필요성
- 애플리케이션과 데이터의 통합
- 개발자들의 시스템 이해
### 5. 데이터 모델링 시 주의할 점
- 중복
- 비유연성
- 비일관성
### 6. 데이터 모델링 단계
가. 개념 데이터 모델링
나. 논리 데이터 모델링
다. 물리 데이터 모델링
### 7. 모델링 기본 원칙
- 커뮤니케이션 원칙
- 모델링 상세화 원칙
- 논리적 표현 원칙
### 8. 좋은 데이터 모델의 요소
- 완정성
- 중복 배제
- 비지니스 룰
- 데이터의 재사용
- 안정성 및 확장성
- 간결성
- 의사소통
- 통합성
## 2. 데이터 모델링 기법 이해
### 1. 데이터 모델 목적
설계에 대한 계획이자 청사진이 곧 데이터 모델의 목적
### 2. 개체-관계 모델 기법
개체-관계 다이어그램(ERD)은 '개체, 관계, 속성' 세 개의 기본 요소를 사용하여 도형으로 표시한 것
### 3. 개체-관계 모델 구성 요소
- 논리 모델링 -> 물리 모델링 단계로 넘어오면서 고려해야 할 사항
- Super/Sub 관계의 엔티티를 몇 개의 테이블로 만들 것인가
- 배타적 관계 엔터니의 외래키를 몇 개로 할것인가
- 성능 향상을 우해 테이블을 추가할 것인가 통합할 것인가?
- 통계 장업을 ㅜ이해 합계 테이블 같은 임시 테이블을 몇 개로 할 것이며 유일키를 무엇으로 할 것인가?
- 컬럼을 다른 테이블에 중복할지 중복하면 앱이 관련되어 있는가? 인덱스 설정, 스냅샷 , 뷰 등읙 ㅐㄱ체가 필요한가?
- 분산 환경에서 테이블을 중복할 것인가, 중앙에 필요 테이블을 따로 가져갈 것인가
- 데이터가 분산 환경에서 이동 시 문제가 어떻게 처리할 것인가
가. 엔터티
나. 속성
다. 식별자
- 본질 식별자
- 후보 식별자
- 대체(보조) 식별자
- 인조 식별자
- 실질 식별자
라. 관계
엔터티 간의 연관성
- 관계 연결
- 일대일
- 일대다
- 다대다
마. 카디날리티
하나의 개체에 대해 다른 엔터티에서 몇 개의 개체가 참여하는지를 나타냄
1개 이상 6개 이하의 과목에 등록할 수 있다면 카디날리티는 (1, 6)이 된다
바. 존재 종속
- 한 엔터티의 존재가 다른 엔터티의 존재에 영향을 받는다면 이를 존재 종속이라 한다.
사. 서브타입
- 전체 집합인 슈퍼타입의 부분 집합이며, 배타적(1개만 관련)/포괄적(1개 이상 관련)으로 구분 한다.
- 슈퍼 타입은 공통적인 모든 속성을 포함하고 있고 서브타입은 각 서브타입에 적절한 속성만 포함한다.
## 3. 데이터 모델링 표기법 이해
### 1. 바커 표기법
가. 엔터티: 반드시 두 개 이상의 속성을 가져야 한다
다. 관계
1) 엔터티와 엔터티 간의 관계
- 1:1 관계
- 1:M 관계
- M:M 관계
2) 엔터티와 엔터티 간 상관 관계의 조건
  - 필수 조건(실선)
  - 선택 조건(점선)
라. 식별자
엔터티를 대표할 수 있는 속성을 의미하며 하나의 엔터티에는 반드시 하나의 식별자가 존재한다.
1) 식별자의 유형
- 본질 식별자
- 후보 식별자
- 대체(보조) 식별자
- 인조 식별자
- 실질 식별자
2) 작성 방법
마. 서브타입
슈퍼 타입 안에 상자로 표시, 중복을 허용하지 않으며 상호 배타적 관계
바. 관계의 표현 비교
### 2. I/E 표기법
가. 엔터티
나. 속성
다. 관계
라. 식별자
마. 서브타입
## 4. 데이터 모델링 사례
### 1. 엔터티 후보 목록 도출
- 현업자료와 현황평가 및 요구사항 정의
- 액티비티 산출물 검토
### 2. 엔터티 정의
- 다른 엔터티와 명확한 구분 가능하며, 주키에 의해 식별 가능
- 속성 존재
- 데이터로서의 중요성과 효용성
### 3. 엔터티 간 관계 정의
- 업무상 연관관계를 갖는 의미 있는 관계 부여
- M:N 관계는 두 개의 1:N 관계로 대체
# 2. 개념 데이터 모델링
## 1. 개념 데이터 모델링 이해
### 1. 개념 데이터 모델 정의
## 2. 주제 영역 정의
### 1. 주제 영역 개념
데이터의 최상위 집합
### 2. 주제 영역 분류 원칙 및 기준
가. 주제 영역 분류 원칙
나. 주제 여역 명명
다. 주제 영역 정의 절차
라. 주제 영역 분류 방법
### 3. 주제 영역 활용
### 4. 주제 영역 정의 내용 도출
## 3. 후보 엔터니 선정
### 1. 후보 엔터티 선정
### 2. 엔터티 후보 수집
### 3. 엔터티 후보 식별
### 4. 엔터티 후보 선정 시 유의사항
### 5. 엔터티 분류
## 4. 핵심 엔터티 정의
### 1. 엔터티 정의의 요건
가. 엔터티 정의
나. 엔터티 정의 시 고려사항
다. 엔터티 파악 요령
라. 엔터티 작성 예
1) 업무 시나리오 작성
2) 엔터티 정의서 작성
마. 엔터티 특징
바. 엔터티 분류
- 유무형에 따른 분류
- 발생 시점에 따른 분류
사. 엔터티 도출 관련 산출물
### 2. 의미상 주어 정의(본질 식별자)
### 3. 코드성 키 엔터티 모델링
### 4. 집합 순수성
가. 집합 순수성의 의미
나. 집합 순수성
다. 집합 순수성 적용 예외 사항
1) 관계의 엔터티화
2) 일부 집합 정의
3) 배타적 관계 대체
### 5. 집합 동질성
가. 집합 동질성 의미
나. 집합 동질성 부여의 예(고객)
### 6. 엔터티 명칭
가. 적절한 엔터티 명칭
나. 엔터티 명칭 부여하기 예제
### 7. 서브타입
가. 서브타입 지정 의의
나. 서브타입 지정 시 고려사항
1) 교집합 허용 불가
2) 서브타입의 합이 전체 집합
3) 서브타입 표현의 기준
   - 개별 속성을 가질 때
   - 개별 관계를 가질 때
   - 가독성을 증진시키고자 할 때
다. 서브타입 도출
1) 분류 속성
2) 다수의 선택적 속성
3) 선택적 관계가 존재하는 경우
4) 도출 절차
   - 분류 속성을 확인
   - 분류 속성값에 의해 분류되는 서브타입을 파악
   - 분류 속성에 따라 필수적/선택적 분할을 정의
   - 서브타입별 속성을 할당
   - 슈퍼타입의 관계를 해당 서브타입에 정의
라. 서브타입의 활용
마. 서브타입의 이해
바. 서브타입명
사. 서브타입세트
아. 서브타입세트명
자. 서브타입 표현의 비교
### 8. 엔터티 통합과 분할
가. 엔터티의 독립성
나. 엔터티 분할/통합
다. 집합 동질성 확대를 통한 집합 통합 사례
라. 유연성 향상을 위한 통합
## 5. 관계 정의
### 1. 관계 정의
개체 간의 관계 또는 속성 간의 관계
- 일대일
- 일대다
- 다대다
### 2. 관계 이해
가. 관계도 집합에 해당
나. 직접 관계를 관계라고 함
두 엔터티 간에는 하나 이상의관계가 존재할 수 있다.
라. 외래키로 정의
마. 관계의 관점
### 3. 관계 표현
가. 관계 형태
1) 하나 이상
2) 단 하나
나. 선택 사양
점선은 존재하지 않을 수 있으며, 직선은 반드시 존재한다.
1) 일반적인 형태
2) 바람직한 형태
다. 두 엔터티 간 연관성 및 관계의 연관도
라. 관계명의 부여
마. 관계 수
### 4. 관계 정의 방법
가. 관계 구문 이해
나. 관계 정의 절차
1) 사원을 주어로 부서를 보는 관계에서 판단해야 할 사항 예제
2) 부서를주어로 사원을 보는 관계에서 판단해야 할 사향 예제
### 5. 관계의 패어링
### 6. 관계의 표기법
### 7. 관계 형태
가. 1:1 관계
나. M:1 관계
1) 특징 : 가장 흔한 형태로..
2) Both Side Mandatory
3) One Optional
4) Many Optional
5) Both Side Optional

다. M:M 관계
라. 다중 관계 처리
마. 특수한 형태 관계
## 6. 관계 데이터 모델
### 1. 관계 데이터 모델 개념
### 2. 관계 데이터 모델 기본 용어
- 릴레이션
- 속성
- 투플
- 도메인
- 널
- 차수
- 카디널리티
### 3. 릴레이션의 구성
- 릴레이션 스키마
### 4. 릴레이션의 특성
- 투플의 유일성
- 투플의 무순서
- 속성의 무순서
- 속성의 원자성
### 5. 데이터베이스의 구성
- 데이터베이스 스키마
- 데이터베이스 인스턴스
### 6. 키(key)
가. 키의 정의
나. 키의 특성
- 유일성
- 최소성
다. 키의 종류
- 슈퍼키
- 후보키기본키
- 대체키
- 외래키
- 릴레이션들 간의 관계를 표현
### 7. 관계 데이터 모델의 제약
가. 무결성 제약조건
나. 개체 무결성 제약조건
다. 참조 무결성 제약조건
# 3. 논리데이터 모델링
## 1. 논리 데이터 모델링 이해
### 1. 논리 데이터 모델링 정의
- 데이터베이스 설계 프로세스의 기초 설계 단계로써 비즈니스 정보의 구조와 규칙을 명확하게 표현할 수 있는기법
- ER다이어그램으로 표현된 개념적 구조를 데이터베이스에 저장할 형태로 표현한 논리적 구조이다.
- 물리적인 스키마(Schema) 설계를 하기 전 단계의 데이터 모델 상태이다.
### 2. 논리 데이터 모델의 구성요소
- 엔터티
- 관계(연관도 및 관계수)
- 수퍼타입 및 서브타입
- 속성 및 도메인
- 주키, 후보키, 데체키, 외래키
- 업무 규칙
### 3. 논리 데이터 모델링 이해
- 개념 데이터 모델링에서 정의한 핵심 엔터티와 관계를 바탕으로 속성을 정의하고 식별자를 확정하는과정
가. 정규화 과정
- 관계형 데이터베이스에서 데이터의 보존성을 높이고, 중복성은 줄여 저장공간의 최소화를 꾀하고 조회속도 등을 향상하기 위한 테이블을 구성하는 일련의 과정
- 정규화는 1~5단계의정규화 과정과 반정규화 과정이 있으며 보통 특수하고 복잡한 자료구조가 아니라면 제 3정규화 과정까지 징행하면 정규화 모델링이 완료
- ...
나. 정규화된 설계의이점
- 기억장소 크기의 최소화
- 데이터 불일치 위험의 최소화
- 수정 및 삭제로 인한 이상 가능성 최소화
- 데이터 구조의 안정성 최대화
다. 제 1정규화
- 속성 원자 값 및 원칙 및 중복성 제거 원칙 및 PK 설정 원칙을 준수
- 모든 엔터티는 유일키이자 데이터 간 구분자 값인 PK가 존재해야 한다.
- 모든 속성 값은 원자성, 즉 단일값 형태로 데이터 형태가 표현되어야 하며 중복되면 안 된다.
- 값이 중복되는 2개 이상 속성들은 별도의 엔터티로분리해야 한다.
라. 제 2정규화
...
마. 제 3정규화
- 이행적 함수의 종속성 제거원칙을 준수
- 기본키에 의존성을 가져야 한다.
- 기본키에 의존하지 않고 일반속성에 의존하는 속성을 제거 또는 분리
...
### 4. 논리 데이터 모델링 목적 및 효과
### 5. 논리 데이터 모델링의 주요 성공 요인
## 2. 속성 정의
### 1. 속성 개념
가. 속성 정의 : 더 이상 분리될 수 없는 최소의 데이터 보관단위이다.
나. 속성 특징
1) 속성의 어원적 의미
2) 속성도 일종의 집합
3) 릴레이션도 속성
4) 속성들 간의 서로 독립적
5) 업무에 필요한 정보
6) 한 개의 속성값만 가지며, 다중값일 경우 별도의 엔터티를 이용하여 분리
다.속성의 분류
- 특성에 따른 분류
- 엔터티 구성방식에 따른 분류
라. 엔터티, 인스턴스, 속성, 속성값의 관계
마. 업무 규칙
### 2. 속성 후보 도출
가. 속성 후보 수집처
나. 속성 후보 선정 원칙
다. 속성의 기본 구성요소
### 3. 속성 검증 및 확정
가. 최소 단위 분할
1) 검증 방법
2) 분할 속성의 대표적 유형
나. 하나의 값(Single Value)만을 가지는지 검증 필요
다. 추출 속성에 따른 검증
### 4. 가공 속성 규칙
### 5. 속성 정의식 유의 사항
가. 명확한 속성 명칭 사용
나. 유일한 복합명사 사용
다. 단수형으로 속성명을 사용함
라. 표준 단어 제정
마. 자의적인 전용 금지
### 6. 속성 업무 규칙 정의
## 3. 엔터티 상세회
### 1. 식별자 확정
가. 엔터티 구체화 4단계
1) 식별자 확정 단계
2) 정규화 단계
3) M:M 관계 해소 단계
4) 참조 무결성 정의 단계
나 본질 식별자
엔터티는 항상 부모가 누구인지를 확인하는 방식으로 진행
1) 키 엔터티의본질 식별자
2) 절대 종속/상대 종속 의미
3) 직접 종속/간접 종속 의미
4) 행위 데이터의본질 식별자
다. 후보 식별자 도출
라. 데체(보조) 식별자
마. 인조 식별자 지정
바. 식별자 확정
### 2. 정규화
가. 정규화의 의미
1) 입력이상
2) 삭제이상
3) 갱신이상
나. 정규화의 장점
1) 중복 값이 줄어든다.
2) NULL값이 줄어든다.
3) 복잡한 코드로 데이터 모델을 보완할 필요가 없다
4) 새로운 요구 사항의 발견 과정을 돕는다
5) 업무 규칙의 정밀한 포착을 보증한다
6) 데이터 구조의 안정성을 최대화한다

다. 정규화 단계
1) 1차 정규형
가) 정의
나)정규화 작업
2) 2차 정규형
가) 정의
나) 정규화 작업
3) 3차 정규형
4) BCNF 정규형
가) 정의
나) 3차 정규화의 문제점
다) 3차 정규형을 만족하고 BCNF가 아닌 경우
### 3. M:M 관계 해소
가. M:M 관계의 의미
나. M:M 관계 해소의 의의
### 4. 참조 무결성 규칙 정의
가. 입력 규칙
나. 삭제규칙
## 4. 이력관리정의
### 1. 이력 관리 정의
### 2. 이력 관리 대상 선정
가. 사용자 조사
나. 이력 데이터의 종류
1) 발생 이력 데이터
2) 변경 이력 데이터
3) 진행 이력 데이터
다. 이력 관리 형태
1) 시점 이력
2) 선분 이력
라. 선분 이력 관리 유형
1) 인스턴스 레벨 이력 관리
2) 속성 레벨 이력 관리
3) 주제 레벨 이력 관리
### 3. 선분 이력 관리용 식별자 확정
가. 선분 이력에서 식별자 결정 시 고려 사항
나. 선분 이력에서 종료점 처리 시 주의 사항
1) 종료점이 미정이므로 NULL
2) 수렴하므로 최대치 부여
# 4. 물리 데이터 모델링
## 1. 물리 데이터 모델링 이해
### 1. 물리 데이터 모델 정의
환경에 맞게
명확하게 지정
전문 모델링 툴
### 2. 물리 데이터 모델 의의
### 3. 논리 모델의 물리 데이터 모델로의 전환
## 2. 물리 요소 조사 및 분석
### 1. 시스템 구축 관련 명명 규칙
### 2. 하드웨어 자원 파악
### 3. 운영체계 및 DBMS 버전 파악
### 4. DBMS 파라미터 정보 파악
### 5. 데이터베이스 운영과 관련된 관리요소 파악
## 3. 논리-물리 모델 변환
### 1. 논리 데이터 모델 - 물리 데이터 모델 변환 용어
### 2. 엔터티 - 테이블 변환
가. 테이블 설명
1) 테이블
2) 로우
3) 칼럼
4) 기본키
5) 외래키
나. 서브타입 변환
1) 하나의 테이블로 통합
가) 절차
나) 테이블 사례
다) 하나의 테이블로의 통합이 유리한 경우
라) 하나의 테이블로의 통합이 불리한 경우
2) 여러 개의 테이블로 분할
가) 절차
나) 테이블 사례
다) 여러 개의 테이블로 분할한 경우가 유리한 경우
라) 여러 개의 테이블로 분할한 경우가 불리한 경우
3) 아크(ARC) 형태로 적용
가) 다음의 여러 가지 경우를 만족할 때 적용한다.
다. 서브타입 변환 예
1) Case 1 : 서브타입을 테이블로 분리
2) Case 2 : 서브타입을 통합 테이블로 생성
라. 테이블 목록 정의서
### 3. 속성 - 칼럼 변환
가. 일반 속성 변환
나. Primary UID -> 기본키(Primary Key)변환
1) 변환 절차
2) 변환 예
다. Primary UID(관계의 UID Bar) -> 기본키(Primary Key)변환
1) 변환 절차
2) 변환 예
라. Secondary(Alternate) UID-> Unique Key 변환
마. 테이블 정의서: 개발자들이 가장 많이 참조하는 산출물 중 하나이다.
### 4. 관계 변환
가. 1:M 관계 변환
1) 변환 절차
2) 변환 예
3) 1:M 관계에서 1쪽에 Mandatory 관계일 때의 변환 시 주의사항
나. 1:1 관계 변환
1) 변환 절차
2) 변환 예 (1:1 Mandatory)
3) 변환 시 주의 사항
다. 1:M 순환 관계 변환
1) 변환 절차
2) 변환 예
라. 배타적 관계 변환
1) 외래키 (Foreign Key) 분리 방법
2) 외래키 결합 방법
### 5. 관리상 필요한 칼럼 추가
### 6. 데이터 타입 선택
가. 개념
나. 문자 타입
다. 숫자 타입
라. 날짜 타입
### 7. 데이터 표준 적용
가. 개념
나. 데이터 표준 적용 대상
다. 데이터 표준 적용 방법
## 4. 반정규화
### 1. 테이블 분할
가. 개념
나. 수평 분할
다. 수직 분할
### 2. 중복 테이블 생성
가. 개념
나. 중복 테이블 생성 판단 근거
다. 중복 테이블 유형
1) 집계(통계) 테이블 추가
가) 집계 테이블 유형
나) 집계 테이블 생성 시 유의사항
2) 진행 테이블 추가
가) 진행 테이블 추가 사항
나) 진행 테이블 생성 시 유의 사항
### 3. 중복 칼럼 생성
가. 개념
나. 중복 칼럼 생성 상황
다. 중복 칼럼 생성 시 유의 사항항




